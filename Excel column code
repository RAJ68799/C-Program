#include <stdio.h>

int main()
{
    int num;
    char str[20];
    scanf("%d", &num);

    for(int i=0;num>0;i++)
    {
        str[i] = (num % 26) + 64;
        num /= 26;
    }

    while(i--)
        printf("%c", str[i]);

    return 0;
}
